{
    "name": "react-default-it",
    "displayName": "Default It (React)",
    "description": "DefaultProps based on PropTypes",
    "version": "0.8.0",
    "publisher": "kserjey",
    "engines": {
        "vscode": "^1.17.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.defaultIt"
    ],
    "main": "./src/extension",
    "contributes": {
        "commands": [
            {
                "command": "extension.defaultIt",
                "title": "Generate defaultProps"
            }
        ]
    },
    "scripts": {
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "vscode": "^1.1.5",
        "eslint": "^4.6.1"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/kserjey/react-default-it.git"
    }
}
